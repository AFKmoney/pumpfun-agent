# Core
python-dotenv>=1.0.0
pyyaml>=6.0
aiohttp>=3.9.0
asyncio-throttle>=1.0.2
tenacity>=8.2.3
websockets>=12.0

# Solana
solana>=0.34.0
solders>=0.21.0
base58>=2.1.1
mnemonic>=0.20

# EVM (Ethereum / Base)
web3>=7.0.0
eth-account>=0.13.0

# Data & Analytics
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.12.0
zstandard>=0.22.0          # bonding curve zstd-compressed account decoding

# Notifications
python-telegram-bot>=21.0

# Monitoring
prometheus-client>=0.20.0
structlog>=24.1.0

# Persistence
# sqlite3 is part of Python stdlib - no install needed

# Backtesting
vectorbt>=0.26.0

# Dashboard (FastAPI)
fastapi>=0.111.0
uvicorn>=0.30.0

# HTTP retries already covered by tenacity

